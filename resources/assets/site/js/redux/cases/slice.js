import { createSlice } from '@reduxjs/toolkit';

const casesSlice = createSlice({
    name: 'casesSlice',
    initialState: {},
    reducers: {},
    extraReducers: {}
});

export default casesSlice;
